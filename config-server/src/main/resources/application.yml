server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sahilrana7582/config-repo
          clone-on-start: true
          searchPaths: ticket-booking-application
  application:
    name: config-server
